# Optional: auto-enter the dev shell when you cd into this repo (needs direnv).
# Prefers the flake; falls back to shell.nix.
if has nix && nix flake metadata &>/dev/null; then
  use flake
else
  use nix
fi
